[
  {
    "type": "function",
    "exact_cache_only": true,
    "function": {
      "name": "control_light",
      "description": "Turn a light on or off, or change brightness.",
      "parameters": {
        "type": "object",
        "properties": {
          "entity_id": {
            "type": "string",
            "description": "The specific entity_id of the light (e.g. light.kitchen_main)"
          },
          "action": {
            "type": "string",
            "enum": ["turn_on", "turn_off"],
            "description": "The action to perform"
          },
          "brightness_pct": {
            "type": "integer",
            "description": "Brightness level as a percentage (0-100). Optional."
          }
        },
        "required": ["entity_id", "action"]
      }
    }
  },
  {
    "type": "function",
    "exact_cache_only": true,
    "function": {
      "name": "set_temperature",
      "description": "Set the target temperature for a climate device.",
      "parameters": {
        "type": "object",
        "properties": {
          "entity_id": {
            "type": "string",
            "description": "The entity_id of the thermostat (e.g. climate.living_room)"
          },
          "temperature": {
            "type": "number",
            "description": "The target temperature to set."
          }
        },
        "required": ["entity_id", "temperature"]
      }
    }
  },
  {
    "type": "function",
    "exact_cache_only": true,
    "function": {
      "name": "play_music",
      "description": "Searches for and plays music using Music Assistant. Targets a specific room or speaker.",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "The song, artist, album, or playlist name."
          },
          "media_type": {
            "type": "string",
            "enum": ["track", "playlist", "album", "artist", "radio"],
            "description": "The type of content."
          },
          "room": {
            "type": "string",
            "description": "The room where the music should play (e.g., 'Wohnzimmer'). If the user doesn't specify, use the room they are currently in."
          }
        },
        "required": ["query", "media_type", "room"]
      }
    }
  },
  {
    "type": "function",
    "exact_cache_only": false,
    "function": {
      "name": "stop_music",
      "description": "Stops or pauses music playback in a specific room or speaker.",
      "parameters": {
        "type": "object",
        "properties": {
          "room": {
            "type": "string",
            "description": "The room where the music should be stopped (e.g., 'Wohnzimmer'). If the user doesn't specify, use the room they are currently in."
          }
        },
        "required": ["room"]
      }
    }
  },
  {
    "type": "function",
    "exact_cache_only": true,
    "function": {
      "name": "activate_scene",
      "description": "Activates a predefined smart home scene (e.g., for cozy lighting, sleeping, waking up, or leaving home). A scene can also be triggered by e.g. Ich gehe schlafen for the scene.schlafen",
      "parameters": {
        "type": "object",
        "properties": {
          "entity_id": {
            "type": "string",
            "description": "The exact entity_id of the scene to activate."
          }
        },
        "required": ["entity_id"]
      }
    }
  },
  {
    "type": "function",
    "exact_cache_only": true,
    "function": {
      "name": "manage_volume",
      "description": "Adjust the local speaker volume of the voice satellite.",
      "parameters": {
        "type": "object",
        "properties": {
          "level": {
            "type": "integer",
            "description": "Target volume level (0-100)."
          },
          "room": {
            "type": "string",
            "description": "The room the voice satellite is placed in"
          }
        },
        "required": ["level", "room"]
      }
    }
  },
  {
    "type": "function",
    "exact_cache_only": true,
    "function": {
      "name": "set_timer",
      "description": "Start a countdown timer on the local device.",
      "parameters": {
        "type": "object",
        "properties": {
          "duration_seconds": {
            "type": "integer",
            "description": "The duration of the timer in seconds. Convert minutes or hours to seconds."
          }
        },
        "required": ["duration_seconds"]
      }
    }
  },
  {
    "type": "function",
    "exact_cache_only": false,
    "function": {
      "name": "next_track",
      "description": "Skips to the next song in a specific room or speaker.",
      "parameters": {
        "type": "object",
        "properties": {
          "room": {
            "type": "string",
            "description": "The room where the music should be skipped (e.g., 'Wohnzimmer'). If the user doesn't specify, use the room they are currently in."
          }
        },
        "required": ["room"]
      }
    }
  },
  {
    "type": "function",
    "exact_cache_only": false,
    "function": {
      "name": "previous_track",
      "description": "Goes back to the previous song in a specific room or speaker.",
      "parameters": {
        "type": "object",
        "properties": {
          "room": {
            "type": "string",
            "description": "The room where the music should go back (e.g., 'Wohnzimmer'). If the user doesn't specify, use the room they are currently in."
          }
        },
        "required": ["room"]
      }
    }
  },
  {
    "type": "function",
    "exact_cache_only": true,
    "function": {
      "name": "queue_music",
      "description": "Searches for and adds music to the end of the queue using Music Assistant. Targets a specific room or speaker.",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "The song, artist, album, or playlist name to add to the queue."
          },
          "media_type": {
            "type": "string",
            "enum": ["track", "playlist", "album", "artist", "radio"],
            "description": "The type of content."
          },
          "room": {
            "type": "string",
            "description": "The room where the music should be queued (e.g., 'Wohnzimmer'). If the user doesn't specify, use the room they are currently in."
          }
        },
        "required": ["query", "media_type", "room"]
      }
    }
  },
  {
    "type": "function",
    "exact_cache_only": false,
    "function": {
      "name": "resume_music",
      "description": "Resumes or unpauses music playback in a specific room or speaker.",
      "parameters": {
        "type": "object",
        "properties": {
          "room": {
            "type": "string",
            "description": "The room where the music should be resumed (e.g., 'Wohnzimmer'). If the user doesn't specify, use the room they are currently in."
          }
        },
        "required": ["room"]
      }
    }
  },
  {
    "type": "function",
    "exact_cache_only": false,
    "function": {
      "name": "whats_playing",
      "description": "Checks what song, artist, or media is currently playing in a specific room.",
      "parameters": {
        "type": "object",
        "properties": {
          "room": {
            "type": "string",
            "description": "The room to check for playing music (e.g., 'Wohnzimmer'). If the user doesn't specify, use the room they are currently in."
          }
        },
        "required": ["room"]
      }
    }
  },
  {
    "type": "function",
    "exact_cache_only": false,
    "function": {
      "name": "clear_queue",
      "description": "Clears the current music queue or playlist in a specific room or speaker.",
      "parameters": {
        "type": "object",
        "properties": {
          "room": {
            "type": "string",
            "description": "The room where the music queue should be cleared (e.g., 'Wohnzimmer'). If the user doesn't specify, use the room they are currently in."
          }
        },
        "required": ["room"]
      }
    }
  },
  {
    "type": "function",
    "exact_cache_only": true,
    "function": {
      "name": "set_timer",
      "description": "Start a countdown timer for a specific room. Provide hours, minutes, and/or seconds.",
      "parameters": {
        "type": "object",
        "properties": {
          "hours": {
            "type": "integer",
            "description": "Number of hours for the timer. Optional."
          },
          "minutes": {
            "type": "integer",
            "description": "Number of minutes for the timer. Optional."
          },
          "seconds": {
            "type": "integer",
            "description": "Number of seconds for the timer. Optional."
          },
          "room": {
            "type": "string",
            "description": "The room where the timer should ring (e.g., 'Wohnzimmer'). If the user doesn't specify, use the room they are currently in."
          }
        },
        "required": ["room"]
      }
    }
  },

  {
    "type": "function",
    "exact_cache_only": false,
    "function": {
      "name": "cancel_timer",
      "description": "Cancels or stops a running timer in a specific room.",
      "parameters": {
        "type": "object",
        "properties": {
          "room": {
            "type": "string",
            "description": "The room where the timer should be cancelled (e.g., 'Wohnzimmer'). If the user doesn't specify, use the room they are currently in."
          }
        },
        "required": ["room"]
      }
    }
  },
  {
    "type": "function",
    "exact_cache_only": false,
    "function": {
      "name": "timer_remaining",
      "description": "Checks how much time is remaining on the currently running timer in a specific room.",
      "parameters": {
        "type": "object",
        "properties": {
          "room": {
            "type": "string",
            "description": "The room to check for an active timer (e.g., 'Wohnzimmer'). If not specified, use the current room."
          }
        },
        "required": ["room"]
      }
    }
  },
  {
    "type": "function",
    "exact_cache_only": false,
    "function": {
      "name": "current_time",
      "description": "Checks the current time in a given timezone"
    }
  }
]
